{
  "name": "company-finder-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port ${PORT:-8080}"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
