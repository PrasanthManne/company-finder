📁 Company Finder

A simple full-stack application to search and filter companies by name, country, industry, tags, and more. Frontend is built using React + Vite, and backend uses JSON Server to serve mock API data.

🚀 Project Structure company-finder/ │── frontend/ # React app (Vite + MUI) │── backend/ # JSON Server backend │ └── db.json # Mock database │── README.md

🎯 Features

Search companies by name

Filter by country, industry, and tags

Responsive UI built with Material UI

Backend using JSON Server (mock API)

Fully deployable on Vercel (frontend) & Render (backend)

🖥️ Frontend (React) 🔧 Setup cd frontend npm install npm run dev

📦 Build npm run build

🗄️ Backend (JSON Server) 🔧 Setup cd backend npm install npx json-server --watch db.json --port 5000

👤 Author

Prasanth Manne Full-Stack Developer

📄 License

MIT License
